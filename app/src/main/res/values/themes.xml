<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        CRITICAL: windowShowWallpaper=true causes the system wallpaper to render
        behind the launcher activity. windowIsTranslucent=true allows the window
        to have transparent regions. Both are required for the frosted glass effect.
    -->
    <style name="Theme.ABLauncher" parent="android:Theme.DeviceDefault.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <!-- null prevents background caching which would block transparency -->
        <item name="android:colorBackgroundCacheHint">@null</item>
        <!-- Makes the system wallpaper render behind this activity -->
        <item name="android:windowShowWallpaper">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
